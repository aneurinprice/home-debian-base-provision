stages:
  - test

test:
  stage: test
  tags:
    - generic
  image: "debian:bullseye-slim"
  script:
    - set -eux
    - bash ./bootstrap.sh
    - su - ansible
    - whoami | grep ansible
    - sudo apt-get update
